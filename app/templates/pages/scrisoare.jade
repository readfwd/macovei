- var titles = [ 'Dragă mamă', 'Dragă tată', 'Dragă buni', 'Dragă bunicule', 'Dragă mătușă', 'Dragă unchiule', 'Draga mea', 'Dragul meu'];
- var destinations = [ 'italia', 'spania', 'franta', 'germania', 'anglia'];

.letters
  .container
    h1 Scrisoare
    .letter
      img#back.hidden-sm.hidden-xs(src='/assets/img/letters/background.png')
      img#backSmall.visible-sm.visible-xs(src='/assets/img/letters/background-small.jpg')
      .letter-overlay.hidden
        h1 Scrisoarea a fost trimisa!
        .hotlinks
          a(href="/scrisoare/") trimite altă scrisoare
      .letter-text
        .title
          .btn-group
            button#title-btn.btn.btn-default.dropdown-toggle(data-toggle='dropdown')
              | Dragă mamă
              span.caret
            ul.dropdown-menu#title-list(role='menu')
              each title,index in titles
                li
                  a.choice(id="title-" + index, data-value=index) #{title}
      .letter-content
        span Mereu ai crezut în mine. Dar mă uit in jurul meu si văd că viitorul pe care mi-l doresc eu nu are nicio șansă in România. Așa că m-am hotărât să plec în #{' '}
        .btn-group(style="display: inline-block")
          button#destination-btn.btn.btn-default.dropdown-toggle(data-toggle='dropdown')
            | Anglia
            span.caret
          span .
          ul.dropdown-menu#destination-list(role='menu')
            each destination,index in destinations
              li
                a.choice(id="destination-" + index, data-value=index) #{destination.toLowerCase().replace(/^.|\s\S/g, function(a) { return a.toUpperCase(); })}
        p O să-mi fie dor de tine, și ție de mine. Dar țara asta pare să fie prea coruptă ca eu să mai pot reuși să fac ceva pe meritul meu, fără pile și fără șpagă. Că nimeni nu pleacă de drag. <br>Să știi, totuși, că mai există un singur om care îmi dă speranță. Este o persoană care mi-a demonstrat că vrea ceea ce vreau și eu. De aceea te rog din suflet ca pe 2 noiembrie să crezi iar în mine și să votezi cu Monica Macovei. <br>Vă iubesc și ne vedem cât de curând!
      .letter-destination
        form.form-horizontal
          .letter-dest-group.control-group
            .controls
              .input-group
                span.input-group-addon Destinatar
                input.form-control(type="email", placeholder="cineva@exemplu.com")
                span.input-group-addon.letter-dest-valid
                  i.fa.fa-check
                span.input-group-addon.letter-dest-warn
                  i.fa.fa-times
      .letter-send
        img(src='/assets/img/letters/sendIt.png')
      .letter-source
        form.form-horizontal
          .letter-dest-group.control-group
            .controls
              .input-group
                span.input-group-addon Expeditor
                input.form-control(type="email", placeholder="cineva@exemplu.com")
                span.input-group-addon.letter-dest-valid
                  i.fa.fa-check
                span.input-group-addon.letter-dest-warn
                  i.fa.fa-times
    .result
      #positive.hidden
        span Mesajul a fost trimis!
      #negative.hidden
        span A aparut o eroare! Te rugăm să reîncerci!
